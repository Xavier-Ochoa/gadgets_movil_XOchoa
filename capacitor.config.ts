import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gadgets.app',
  appName: 'Gadgets Tecnológicos',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
